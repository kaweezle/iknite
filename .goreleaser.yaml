# cSpell: disable
# This is an example .goreleaser.yml file with some sensible defaults.
# Make sure to check the documentation at https://goreleaser.com
version: 2
env:
  - KUBERNETES_VERSION={{ if index .Env "KUBERNETES_VERSION"  }}{{ .Env.KUBERNETES_VERSION }}{{ else }}1.34.3{{ end }}
before:
  hooks:
    - go mod tidy
builds:
  - main: ./cmd/iknite/iknite.go
    env:
      - CGO_ENABLED=0
    goos:
      - linux
    goarch:
      - amd64
    mod_timestamp: "{{ .CommitTimestamp }}"
    flags:
      - -trimpath
    ldflags:
      - -s -w -X github.com/kaweezle/iknite/pkg/cmd.IkniteVersion={{ .Version }}
        -X github.com/kaweezle/iknite/pkg/cmd.Commit={{ .Commit }} -X
        github.com/kaweezle/iknite/pkg/cmd.BuildDate={{ .CommitDate }}
        -X github.com/kaweezle/iknite/pkg/cmd.BuiltBy=goreleaser
        -X github.com/kaweezle/iknite/pkg/apis/iknite/v1alpha1.KubernetesVersionDefault={{ .Env.KUBERNETES_VERSION }}
    binary: iknite
    id: iknite
archives:
  - name_template: >-
      {{ .ProjectName }}_
      {{- title .Os }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else if eq .Arch "386" }}i386
      {{- else }}{{ .Arch }}{{ end }}
      {{- if .Arm }}v{{ .Arm }}{{ end }}
checksum:
  name_template: SHA256SUMS
snapshot:
  version_template: "{{ incpatch .Version }}-devel"
changelog:
  sort: asc
  filters:
    exclude:
      - "^docs"
      - "^test"
      - "^ci"
      - "^refactor"
      - Merge pull request
      - Merge branch
      - go mod tidy
nfpms:
  - formats: [apk]
    license: Apache 2.0
    package_name: iknite
    file_name_template: >-
      {{ .PackageName }}-
      {{- .Version }}.
      {{- if eq .Arch "amd64" }}x86_64
      {{- else if eq .Arch "386" }}i386
      {{- else }}{{ .Arch }}{{ end }}
      {{- if .Arm }}v{{ .Arm }}{{ end }}
      {{- if .Mips }}_{{ .Mips }}{{ end }}
    maintainer: Kaweezle <kaweezle-devel@kaweezle.com>
    description: Start Kubernetes in Alpine Linux
    homepage: https://github.com/kaweezle/iknite
    dependencies:
      - openrc
      - ip6tables
      - containerd
      - kubelet
      - kubeadm
      - kubectl
      - cni-plugins
      - cni-plugin-flannel
      - git
      - openssh
      - util-linux-misc
      - buildkit
      - buildctl
      - nerdctl
    bindir: /sbin
    contents:
      - src: packaging/apk/iknite/crictl.yaml
        dst: /etc/crictl.yaml
      - src: packaging/apk/iknite/iknite.d/kustomization.yaml
        dst: /etc/iknite.d/kustomization.yaml
      - src: packaging/apk/iknite/iknite.d/base/kustomization.yaml
        dst: /etc/iknite.d/base/kustomization.yaml
      - src: packaging/apk/iknite/iknite.d/base/kube-vip-addresspool.yaml
        dst: /etc/iknite.d/base/kube-vip-addresspool.yaml
      - src: packaging/apk/iknite/iknite.d/base/kube-flannel.yaml
        dst: /etc/iknite.d/base/kube-flannel.yaml
      - src: packaging/apk/iknite/iknite.d/base/coredns.yaml
        dst: /etc/iknite.d/base/coredns.yaml
      - src: packaging/apk/iknite/init.d/iknite
        dst: /etc/init.d/iknite
      - src: packaging/apk/iknite/conf.d/iknite
        dst: /etc/conf.d/iknite
      - src: packaging/apk/iknite/flannel/10-flannel.conflist
        dst: /etc/cni/net.d/10-flannel.conflist
      - src: packaging/apk/iknite/flannel/subnet.env
        dst: /lib/iknite/flannel/subnet.env
      - src: packaging/apk/iknite/buildkit/buildkitd.toml
        dst: /etc/buildkit/buildkitd.toml
    apk:
      signature:
        key_file: kaweezle-devel@kaweezle.com-c9d89864.rsa
        key_name: kaweezle-devel@kaweezle.com-c9d89864.rsa.pub
release:
  extra_files:
    - glob: ./packaging/rootfs/base/kaweezle-devel@kaweezle.com-c9d89864.rsa.pub
